services:
  - type: web
    name: backtestgpt-backend
    env: python
    plan: free   # or starter, standard, etc.
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000